# Vendored upstream provenance

`upstream/SymboleoAC-IDE/` is a **vendored copy** (plain files, no nested
`.git`) of:

- Repo:   https://github.com/Smart-Contract-Modelling-uOttawa/SymboleoAC-IDE
- Branch: main
- Commit: dec00792f50334486d73b5efa01469c053243629

## Local modifications on top of that commit

These are intentional and required for SymboleoAC-Web to build; they are NOT in
the upstream repo at the pinned commit:

1. **`ca.uottawa.csmlab.symboleo.ide/src-gen/`** — the IDE content-assist ANTLR
   parser + `AbstractSymboleoIdeModule`, generated by running
   `GenerateSymboleo.mwe2` in Eclipse (upstream `.gitignore`s `src-gen/` and
   never committed the `.ide` one). Powers LSP completion. See
   `language-server/IDE-COMPLETION-BOOTSTRAP.md`.
2. **`ca.uottawa.csmlab.symboleo/META-INF/MANIFEST.MF`** — added
   `org.apache.commons.logging` to `Import-Package` so MWE2 runs in Eclipse.
3. **`.project` / `.classpath`** for all five `ca.uottawa.csmlab.symboleo*`
   projects — upstream `.gitignore`s these; regenerated so the projects import
   into Eclipse for any future MWE2 re-runs.

## Re-syncing with upstream

To pull a newer SymboleoAC-IDE: re-clone at the desired commit, then re-apply
the three modifications above (the `.ide/src-gen` requires re-running MWE2 in
Eclipse if the grammar changed). Update the commit SHA in this file.
